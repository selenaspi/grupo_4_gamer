<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/css/styleregistro.css">
    <link rel="stylesheet" href="/css/stylelogin.css">
</head>
<body>

    <%- include('../partials/header') %>

    
    <main class="Form-register">


        <form action="/users/<%=ruta%>" method="POST" enctype="multipart/form-data">
          <h4>Formulario de Edición del perfil</h4>
      
      
          <input class="Controls" type="text" name="name" id="Nombres"value="<%= metodo === 'POST' ? '' :user.name%>"
          placeholder="Ingrese su Nombre" required/>

        <input class="Controls" type="text" name="lastName" id="Apellido" value="<%= metodo === 'POST' ? '' :user.lastName%>"
              placeholder="Ingrese su Apellido" required/>

        <input class="Controls" type="email" name="email" id="Correo electrónico"
           value="<%= metodo === 'POST' ? '' :user.email%>"
              placeholder="Ingrese su Email" required/>

        
        <input class="Controls" type="number" name="phone" id="Teléfono"value="<%= metodo === 'POST' ? '' :user.phone%>"
              placeholder="Teléfono" required/>

        <input class="Controls" type="password" name="password" id="Contraseña"value="<%= metodo === 'POST' ? '' :user.password%>"
              placeholder="Contraseña" required/>

       
              <div class="c-photos">
                <label for="photo">Foto de usuario</label>
                <input type="file" name="image" accept="image/*" id="photo" class="controls" <%=metodo==="POST"?"required":""%>>
              </div>
        <p> Acepto los <a href="#">términos y condiciones</a></p>
        <input class="botons" type="submit" value="Aceptar"></form>
    </main>

    <%- include('../partials/footer') %>
    
</body>

</html>