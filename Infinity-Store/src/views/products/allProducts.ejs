<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/css/styleAllProducts.css">
    <title>Product List Infinity Store</title>
</head>
<body>
    <%- include('../partials/header') %>
    <main>
        <section class="similares">
            <% for(let i = 0; i < similares.length; i++) { %>
                <div class="articulo">
                <img src="/images/products/<%=similares[i].image%>" alt="Foto Similares">
                <div class="linea"></div>
                <p class="producto-similar1"><a href="/product/<%=similares[i].id%>/details"> <%=similares[i].name.substring(0, 20)%>... </p></a>
                <p class="valor2">$<%= similares[i].price %> </p>
            </div>
            <% } %> 
        </section>
    </main>
    <div class="footer">
    <%- include('../partials/footer') %>
    </div>
</body>
</html>